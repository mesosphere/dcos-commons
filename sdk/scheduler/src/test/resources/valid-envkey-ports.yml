name: "data-store"
pods:
  meta-data:
    count: 2
    tasks:
      some-task:
        goal: RUNNING
        cmd: "some command"
        ports:
          http:
            port: 8080
            env-key: some-key
          no-key:
            port: 8088
          key-key:
             port: 8089
