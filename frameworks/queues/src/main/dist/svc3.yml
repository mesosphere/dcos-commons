name: job3
scheduler:
  user: {{FRAMEWORK_USER}}
pods:
  pod3:
    count: 2
    tasks:
      node:
        goal: RUNNING
        cmd: "echo queue3 >> output && sleep $SLEEP_DURATION"
        cpus: {{NODE_CPUS}}
        memory: {{NODE_MEM}}
        env:
          SLEEP_DURATION: {{SLEEP_DURATION}}
