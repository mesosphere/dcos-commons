name: "hello-world"
pods:
  pod-type:
    count: 1
    tasks:
      test-task:
        goal: RUNNING
        cmd: "./task-cmd"
        endpoints:
          test-port-name:
            port: 0
            env-key: TEST_PORT_1
          test-port-name2:
            port: 0
            env-key: TEST_PORT_2
