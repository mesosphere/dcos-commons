name: {{FRAMEWORK_NAME}}
scheduler:
  principal: {{FRAMEWORK_PRINCIPAL}}
pods:
  hello:
    count: 1
    user: {{FRAMEWORK_USER}}
    tasks:
      server:
        goal: RUNNING
        cmd: "exit 1"
        cpus: {{HELLO_CPUS}}
        memory: {{HELLO_MEM}}
