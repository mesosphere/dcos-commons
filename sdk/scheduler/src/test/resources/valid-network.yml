name: "valid-container-network-test"
pods:
  server:
    count: 1
    # Define networks directly on the pod
    networks:
      test:
        host-ports: [4040]
        container-ports: [4041]
    tasks:
      server:
        goal: RUNNING
        cmd: "cmd"
        cpus: 1
        memory: 1024