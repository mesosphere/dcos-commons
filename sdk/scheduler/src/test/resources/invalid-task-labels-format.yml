name: "invalid-task-labels-format-test"
pods:
  server:
    count: 1
    tasks:
      server:
        goal: RUNNING
        cmd: "cmd"
        cpus: 1
        memory: 1024
        labels: "label1,label1-value"
