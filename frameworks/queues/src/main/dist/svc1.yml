name: job1
scheduler:
  user: {{FRAMEWORK_USER}}
pods:
  pod1:
    count: 2
    tasks:
      node:
        goal: RUNNING
        cmd: "echo queue1 >> output && sleep $SLEEP_DURATION"
        cpus: {{NODE_CPUS}}
        memory: {{NODE_MEM}}
        env:
          SLEEP_DURATION: {{SLEEP_DURATION}}
