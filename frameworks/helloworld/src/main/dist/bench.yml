# This service generates endlessly crashing tasks which do NOT complete their Steps
name: {{FRAMEWORK_NAME}}
pods:
  test:
    count: {{COUNT}}
    tasks:
      server:
        goal: RUNNING
        cmd: echo {{COUNT}} pods strategy {{STRATEGY}} sleeping {{SLEEP}} && sleep {{SLEEP}}
        cpus: {{CPUS}}
        memory: {{MEM}}
plans:
  deploy:
    phases:
      test:
        strategy: {{STRATEGY}}
        pod: test
