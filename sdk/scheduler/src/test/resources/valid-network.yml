name: "valid-container-network-test"
pods:
  server:
    count: 1
    # Define networks directly on the pod
    networks:
      test: {}
    tasks:
      server:
        goal: RUNNING
        cmd: "cmd"
        cpus: 1
        memory: 1024