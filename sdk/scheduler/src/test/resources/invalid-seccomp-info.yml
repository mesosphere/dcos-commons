name: "invalid-seccomp-info"
pods:
  meta-data:
    seccomp-unconfined: true
    seccomp-profile-name: "foobar"
    count: 2
    tasks:
      some-task:
        goal: RUNNING
        cmd: "some command"
        cpus: 1